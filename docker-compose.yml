services:
  gateway:
    container_name: lingopi-gateway
    image: "lingopi-gateway"
    restart: always
    networks:
      - lingopi-backend
    ports:
      - 45000:5000
    build:
      context: ./src
      dockerfile: Lingopi.Gateway/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=$ENV
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - redis

  identity:
    container_name: lingopi-identity
    image: "lingopi-identity"
    restart: always
    networks:
      - lingopi-backend
    build:
      context: ./src
      dockerfile: Lingopi.Identity/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=$ENV
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - redis

  lingo:
    container_name: lingopi-lingo
    image: "lingopi-lingo"
    restart: always
    networks:
      - lingopi-backend
    build:
      context: ./src
      dockerfile: Lingopi.Lingo/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=$ENV
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - redis

  redis:
    container_name: lingopi-redis
    image: redis:7-alpine
    restart: always
    networks:
      - lingopi-backend
    volumes:
      - lingopi-redis-data:/data
    command: redis-server --appendonly yes

volumes:
  lingopi-redis-data:
    driver: local

networks:
  lingopi-backend:
    name: lingopi-backend
    driver: bridge
